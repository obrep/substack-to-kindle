version: '3.9'

services:
  app:
    image: substack-to-kindle
    build: .
    container_name: substack-to-kindle
    restart: always
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./output:/app/output
